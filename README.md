# WebScrap
Python webscraping code
code meant to extract the CIIU code from entities, enterpises, in Colombia. Once the data is stored in a dictiorany the structure of the saved html document needs to be looked at 
in order to extract the desired info. The user agents need to be changed at random in order to prevent overload of the server
